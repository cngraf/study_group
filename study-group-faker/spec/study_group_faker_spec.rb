require 'spec_helper'

describe StudyGroupFaker do
  it 'has a version number' do
    expect(StudyGroupFaker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
